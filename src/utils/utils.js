export const indexedDb = () =>{
  const indexedDB = window.indexedDB || window.webkitIndexedDB || window.mozIndexedDB || window.msIndexedDB
  return indexedDB
}
export const createDb = (db, dbName,version) =>{
  

}